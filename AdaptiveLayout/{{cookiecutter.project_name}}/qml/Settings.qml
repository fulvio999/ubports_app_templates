import QtQuick 2.4
import Qt.labs.settings 1.0

Settings {

    //if 'true' means that is the first time that the user open MyFinance: is necessary insert default data in the Database
    property bool isFirstUse:true;

}
